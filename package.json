{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "nuxt": "^3.7.1",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "lint-staged": {
    "*": "prettier --write"
  }
}
