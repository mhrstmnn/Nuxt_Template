{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@unocss/nuxt": "^0.57.1",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.2",
    "nuxt": "^3.8.0",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "lint-staged": {
    "*": "prettier --write"
  }
}
